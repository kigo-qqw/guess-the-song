pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'guess-the-song'

include 'core'
include 'server'
include 'client'
